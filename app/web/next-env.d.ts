// This file is maintained by Next.js after dependencies are installed.
// Keeping it declaration-free prevents IDE false positives before node_modules
// exists. `next dev` / `next build` will regenerate the standard references.
export {};
