/**
 * The Next.js application lives in app/web/src/app.
 *
 * This file intentionally contains no application code. It exists only to
 * neutralize an older misplaced root-level layout.tsx that some local copies
 * of the project still contain.
 */
export {};
