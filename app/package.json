{
  "name": "mineaclenet-app",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "web"
  ],
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "npm run dev --workspace=mineaclenet-web",
    "build": "npm run build --workspace=mineaclenet-web",
    "start": "npm run start --workspace=mineaclenet-web",
    "typecheck": "npm run typecheck --workspace=mineaclenet-web"
  }
}
