# /etc/sudoers.d/mineacle-deploy   (install with: sudo visudo -f /etc/sudoers.d/mineacle-deploy)
# Lets the deploy script restart the site and nothing else.
mineacle ALL=(root) NOPASSWD: /usr/bin/systemctl restart mineacle-web
